// Gallery App
const GalleryApp = {
    init() {
        // Gallery is mostly static, just add some interactivity
    },
    
    openPhoto(index) {
        alert(`Opening photo ${index + 1}\n(Gallery viewer coming soon)`);
    }
};

GalleryApp.init();
